# 20d process and environment maintenance receipt

Evidence-record: 20d
Date: 2026-09-07 UTC, 2026-09-06 owner-local date.
Result: Passed. Process cleanup, dependency setup, documentation build, clock repair, and bounded clock preflight completed.

## Changes and evidence

The existing execution contract owns one reviewable iteration, bounded helpers,
source ownership, and task cleanup. The existing private handoff shrank from
813 to 103 lines while retaining runtime integration limits and unresolved
advisor findings. Its source index routes to detailed canonical receipts.
The GUI plan's current-state view shrank from 59 to 9 entries. The existing change history retains the previous state.

D28 records iterative maintenance. D29 records an installable work tool for
professionals across coding, research, writing, and second-brain work. The GUI directs
work and presents results. The website explains setup, connections, requirements,
and limits. Existing subscription connections are a desired experience, not an
accepted authentication or runtime capability.

The source-navigation check returned 16 records, 23 typed edges, 11 locators,
and zero broken references. The existing graph remains the navigation owner.
The private Markdown/HTML renderer checked 25 documents and two root link sources.
Handoff writing lint passed at 0.59 violations per 100 words against a 2.0 target.

## Environment completion: 2026-09-07

The owner requested continuation and approved the prepared Habitat setup.
The helper now transfers source through stdin because Habitat cannot read the
Windows archive path directly. It uses one reusable build directory, the same
verified daemon, and the pinned image. Tracked source commit
`8511623f8fd9f3a9f6acbc1346c231f104c5c8d8` supplied the build inputs.

- `npm audit --package-lock-only --ignore-scripts`: zero vulnerabilities.
- `npm ci --ignore-scripts --no-fund`: installed 366 packages, audited 367, zero vulnerabilities.
- `npm run build`, offline: passed, 33 static pages and the search index generated.
- `npm run test:links`, offline: checked 2,752 local references and 1,553 anchors across 33 HTML files, zero failures.
- Administrator `Start-Service W32Time` followed by `w32tm /resync`: succeeded with the existing `time.windows.com,0x9` source. Status reported synchronized stratum 5 and no leap warning.
- A fresh offline container with the pinned image ran a 60-second clock check with 220 subprocess samples. Wall time never decreased and monotonic time advanced. The container removed itself on exit.

The first Administrator resync encountered a stopping service. Starting that
same service before retrying fixed the attempt. No startup policy, time source,
registry, or protection setting changed. Windows status and clock-probe output
remain with the private maintenance helper.

The documentation build emitted a fallback-language warning for a `gitignore`
code block and a missing custom 404 content message. The build and link check
passed. These messages do not establish a broken product route.

This is a meaningful environment intervention followed by a clean preflight,
not complete 20c acceptance. One fresh strict full-suite attempt can resume.
It must verify current source hashes, daemon/image identity, mounts, and process
limits first. Any clock discontinuity stops that attempt. Independent acceptance
follows only a passing author run. No live model call or full 20c suite ran here.

The build directory is a named reusable documentation environment, including
its dependency cache and built output. It is not a per-attempt source copy.
All build/probe containers remove themselves. The maintenance owner retains the
helper and concise evidence for reuse. Disposable transfer files are removed.

## Cleanup

The cleanup helper verified the two retained exports and the exact existing
resource inventories. It removed 45 stopped task containers and kept one exited
clock-failure container. The private cleanup receipt owns path removals, measured
bytes, archive manifests, residuals, and removal conditions. It supersedes the
older inventories' retention disposition. Shared images and unrelated work are
outside this cleanup.

## Independent review and continuation

The independent reviewer checked continuation, authority, document links, and
evidence claims. Review identified stale setup authority, a missing receipt,
a JSON link outside the document checker's coverage, and the packet's inaccurate
inspection-only classification. The agent corrected these in the existing owners.

Packet 20d completed its maintenance scope. Packet 20c is ready for one strict
acceptance attempt after checking the current source and boundary. Its full
runtime acceptance and the separate live-token gate remain open.

Planning checks, source navigation, tracked-file line endings, diff whitespace,
and the private plan/document render checks passed after the review corrections.


Final cleanup removed 45 stopped containers, 82 Habitat directories, and 72
redundant Windows evidence targets. Measured logical data removed totals
57,266,839 bytes. Final inspection found one retained failed-clock container
and its two source/test directories. Both verified exports remain intact.
A 19-file source mirror remains because five documentation files are not direct
archive members. The runtime packet owner reviews those residuals by 2026-09-13
or after the next clock-repair iteration, whichever comes first.

Final resource inspection found no retained build or clock-probe containers.
The reusable documentation environment occupies 435 MiB, including installed
dependencies, cache, source, and built output. The documentation-build owner
reuses this one directory and reviews it when its lockfile or purpose changes.
